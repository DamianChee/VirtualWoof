import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#a23d30",
    },
    secondary: {
      main: "#775651",
    },
  },
});

export default theme;
